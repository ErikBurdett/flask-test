{
    "environments": {
      "test": {
        "scripts": {
          "test-setup": "pip install -r requirements.txt",
          "test": "python -m unittest test_script.py"
        }
      }
    }
  }
